import React from 'react';
import {Text, View} from 'react-native';

const Details = () => {
    return (
        <View style={{flex: 1, backgroundColor: "#7F3589"}}>
            <Text style={{ flexDirection : "row",color: "white"}}>Detail screen</Text>
        </View>
    );
}

export default Details;
